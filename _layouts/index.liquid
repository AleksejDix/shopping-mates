---
layout: default
---

<section>
{% for page in site.posts limit:1 %}
{% if page["header image"] contains "cdn.siroop.ch" %}
  {% assign headerImage = page["header image"] %}
{% else %}
  {% assign headerImage = page["header image"] | prepend:site.baseurl %}
{% endif %}

  <div class="ratio ratio--2x1">
    <img class="ratio-content vertical-center" src="{{ headerImage }}" alt="">
    <div class="ratio-content post-wrap">
      <div class="page-width">
      <a href="{{ page.url | prepend:site.baseurl }}">
        <h1 class="title title--separator title--h1 title--white">
          {{ page.title }}
        </h1>
      </a>
        {% include post-meta.liquid %}

      </div>
    </div>
  </div>

{% endfor %}
</section>


<div class="page-width">
  <h1 class="title title--separator title--h1">
    {{ page.title }}
  </h1>

  <div class="grid grid--waffle">
    {% for page in site.posts offset:1 limit:3 %}
      <div class="grid__item grid__item--1-3">
          {% include post.liquid  %}
      </div>
    {% endfor %}
  </div>

</div>

{% if site.mates.size > 0 %}
<div class="page-width">
  <h2 class="title title--separator title--h1">
    Die Shopping Mates
  </h2>
  <div class="grid grid--waffle">
  {% assign mates = site.mates | sort: 'position' %}
  {% for mate in mates %}

    <div class="grid__item grid__item--1-3">
      <a href="{{ mate.url | prepend: site.baseurl }}">
        <img src="{{ mate.image | prepend: site.baseurl }}" alt="{{ mate.title }}">
      </a>
    </div>
  {% endfor %}
  </div>
</div>
{% endif %}


<a href="{{ "/feed.xml" | prepend: site.baseurl }}">via RSS</a>
